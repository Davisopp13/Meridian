# Meridian — Ralph Progress Log
# This file is the running record of Ralph runs against this repo.
# New runs append below; do not delete prior runs.

═══════════════════════════════════════════════════════════════════
CURRENT RUN — Pause Feature v2.1
═══════════════════════════════════════════════════════════════════

Started:    04/27/2026, evening (ET)
Branch:     feat/pause-feature-v2
PRD:        PRD.md (v2.1, Ralph-ready)
Sources:    Lish conversation 04/26 (semantic distinction)
            Davis code audit commit b2ac0fb (state-shape findings)

Goal:
  Re-separate Pause from Awaiting as distinct states (Wave 1A) and
  add process-pause to the MPL bar (Wave 1B).

═══════════════════════════════════════════════════════════════════
PRIOR WORK (one-line history; full detail in git log)
═══════════════════════════════════════════════════════════════════

  Tier I features         — shipped 04/23/2026
                            Notes on events/entries, MPL recent pairs,
                            CT/MPL call button, admin team selector
                            Migrations 023–027

  Tier 1.5 — MPL bar      — shipped 04/23/2026
                            Stat tray merged, pill colors, base width 380
                            (Single commit, no migration)

  Quick Log timer bug     — open since 04/18/2026
                            Wanda reported. Active timer not stopped on
                            Quick Log. Not in scope of this run.

═══════════════════════════════════════════════════════════════════
RUN PROGRESS — Pause Feature v2.1
═══════════════════════════════════════════════════════════════════

[Ralph: append your work below as you go.
Required entries per PRD §8:
  - Each task started/completed
  - Each migration number used
  - Each file edited (full path)
  - Any ambiguity-driven decisions (so Davis can review)
  - Any blockers — STOP if blocked, do not guess]

## Iteration 1 - Wave 1A: Case-pause cleanup (CT)
- Implemented full Wave 1A: separate Pause from Awaiting as distinct, independent states
- No migration needed: ct_cases.status is text/varchar (confirmed by absence of enum in migrations)
- Files changed:
  - src/ct/CtApp.jsx
  - src/ct/CtPipBar.jsx
  - src/components/MinimizedStrip.jsx
  - PRD.md (marked Wave 1A tasks complete)
- What was implemented:
  - restoreCaseFromRow: now handles status='paused' → {paused:true, awaiting:false};
    uses stored duration_s for paused cases (avoids wall-clock overcount after resume)
  - handleTruePauseCase: writes status:'paused', persists duration_s=c.elapsed, stops timer
  - handleResumeFromPauseCase: writes status:'active', clears flags, starts timer
  - handleAwaitingCase (renamed from handlePauseCase): writes status:'awaiting',
    sets {paused:false, awaiting:true} — flags are now independent
  - handleResumeAwaitingCase (renamed from handleResumeCase): clears both flags, starts timer
  - handleResumeCase: smart router — delegates to awaiting or pause handler by case state
    (kept so SwimlaneTray's onResumeCase prop still works without touching out-of-scope file)
  - Deleted duplicate handleAwaitingCase that was at old line 868
  - MinimizedStrip: two buttons (⏸ Pause + ⏳/Clock Awaiting), each with title tooltip
    Pause button: green when paused (shows ▶), white when active (shows ⏸)
    Awaiting button: amber when awaiting (shows ▶), white when active (shows ⏳)
  - CtPipBar: passes onAwaitingCase + onResumeAwaitingCase to MinimizedStrip
- Decisions made (for Davis review):
  - handleTruePauseCase writes duration_s=c.elapsed so elapsed survives tab close while paused.
    The restoreCaseFromRow then uses duration_s directly for paused cases (no timestamp math).
    This avoids needing a new paused_since column. PRD said "no migration if text column" and
    did not mention a new column for paused state, so this is the minimal approach.
  - handleResumeCase is kept as a smart router (not renamed) so SwimlaneTray's onResumeCase
    prop keeps working without touching SwimlaneTray.jsx (which is out of PRD scope §7).
  - Mutual exclusion: both handlers clear the other flag before setting their own.
    e.g. handleTruePauseCase sets {paused:true, awaiting:false}.
- Build: npm run build passes clean (✓ 2518 modules, 1.45s)
- Learnings for future iterations:
  - ct_cases.status is a plain text column; no migration needed to add 'paused' value
  - SwimlaneTray.jsx is out-of-scope per §7 — work around it with smart-router pattern
  - PillZone.jsx already had onAwaitingCase wired; no changes needed there
---

## Iteration 3 - Wave 1B: Multicase visual layout (CT)
- Implemented compact tab row for 3+ cases in CtPipBar, completed remaining Wave 1B test items
- Files changed:
  - src/ct/CtPipBar.jsx
  - PRD.md (marked 6 remaining Wave 1B tests complete)
- What was implemented:
  - CtPipBar now renders two different case displays based on count:
    * 1-2 cases: existing PillZone (unchanged)
    * 3+ cases: inline compact tab row — one button per case showing status dot + case number.
      Dot color: amber (#fbbf24) for awaiting, gray (#6b7280) for paused, orange (#e8540a) for active.
      Focused tab gets orange border/background; unfocused is dimmed. Click any tab → onFocusCase.
      Tray chevron (▲/▼) appended at end of tab row. maxWidth: 220px with overflow:hidden
      so 4+ cases clip gracefully (tray/SwimlaneTray handles all-case navigation regardless).
  - 3+ case tray (SwimlaneTray → CasesLane → CaseLaneRow) provides "focused expanded" view
    with full action buttons; all existing cases are clickable for focus within the tray.
- Behaviors already passing (verified by code analysis, no code change needed):
  - At 1 case: focused CasePill = single-card layout (existing) ✓
  - At 2 cases: PillZone shows both pills; unfocused pill click → onFocusCase; focused pill
    shows ⏸/× actions; unfocused shows none. "Both visible, clickable focus, actions on focused" ✓
    Note: unfocused pill does not show elapsed time (CasePill.jsx is out of PRD §7 scope).
    Davis may want to improve this in a future iteration.
  - Both timers tick simultaneously: each case gets its own setInterval in caseTimers ref ✓
  - Focus doesn't modify pause/awaiting: handleFocusCase is just setFocusedCaseId(id) ✓
  - focusedCaseId persists: already stored in localStorage with cases/trayOpen (CtApp.jsx L285) ✓
- Decisions made (for Davis review):
  - Kept PillZone for 1-2 cases (no layout regression, awaiting pill resumes accessible).
  - 3+ case tab row uses maxWidth:220 + overflow:hidden rather than widening the window.
    Widening for 3+ cases would require overriding pin() which risks mismatches on fast
    state transitions (cases.length in pin() can be stale at call time). Tray compensates.
  - Awaiting resume button appears on unfocused CasePill (via PillZone) for the 2-case layout.
    This technically breaks "actions only on focused" for awaiting state but CasePill.jsx is
    out of scope. Functionally it's a useful affordance; Davis can decide if it's a problem.
- Build: npm run build passes clean (✓ 2518 modules, 1.42s)
- Learnings for future iterations:
  - CT_HEIGHTS and getCtBarWidth are exported from constants.js and importable in CtApp.jsx
    if future work needs to compute 2-case or 3+-case specific window sizes.
  - CasePill.jsx (src/components/) is out of PRD §7 scope — improvements to unfocused pill
    display (elapsed time, pause state indicator) need a new iteration or scope expansion.
  - For 4+ cases, the compact tab row clips (maxWidth:220 is ~3 nine-digit case numbers).
    SwimlaneTray → CasesLane shows all cases with focus switching regardless.
---

## Iteration 2 - Wave 1B: Bookmarklet deduplication (CT)
- Implemented case-number dedup guard in handleCaseStart
- Files changed:
  - src/ct/CtApp.jsx
  - PRD.md (marked 2 bookmarklet tests complete)
- What was implemented:
  - Added `cases.find(c => c.caseNum === caseNumber)` guard at top of handleCaseStart.
    If caseNumber already active → setFocusedCaseId(existing.id) and return. No new DB row.
  - "A keeps ticking" was already correct: handleCaseStart never stopped existing timers,
    and startCaseTimer is a no-op if a timer already runs for that id.
  - Bookmarklet deduplication was the only missing piece for both §5.1 tests.
- Decisions made (for Davis review):
  - Matched on caseNum (display number) not session id — two sessions with same case_number
    but different IDs are treated as the same case for refocus purposes. This matches PRD §5.1:
    "If the case id matches an existing case in the array → focus that case."
  - No toast or log on refocus — PRD §9: "Do not add a prompt or toast on bookmarklet handoff."
- Build: npm run build passes clean (✓ 2518 modules, 1.37s)
- Learnings for future iterations:
  - cases state IS accessible in handleCaseStart closure because usePendingTriggers stores
    handlers in a ref (handlersRef.current = handlers) that updates every render cycle.
  - The "A keeps ticking" requirement was already satisfied before this iteration — the
    prior code never called stopCaseTimer on case A when adding case B.
---

## Iteration 4 - Wave 1C: Process-pause on MPL bar
- Implemented process-pause for MPL bar (both MplApp and DashboardApp parents)
- Migration number used: 028
- Files changed:
  - supabase/migrations/028_mpl_process_pause.sql (new)
  - src/mpl/MplApp.jsx
  - src/mpl/MplPipBar.jsx
  - src/DashboardApp.jsx
  - PRD.md (marked Wave 1C + system-wide tests complete)
- What was implemented:
  - Migration 028: adds paused_at TIMESTAMPTZ NULL and pause_seconds_v2 INTEGER DEFAULT 0
    to mpl_active_timers. Used pause_seconds_v2 (not total_paused_seconds) per PRD §6.4 — no
    read sites for total_paused_seconds found in src/ and no migration creates it.
  - handleProcessPause(id): stopTimer → set {paused:true, pausedAt:now} in state → upsert
    paused_at+status='paused' to DB. Guard: no-op if already paused or no user.
  - handleProcessResume(id): compute pause duration from proc.pausedAt, accumulate in
    pausedSeconds → update state {paused:false, pausedAt:null, pausedSeconds} → startTimer →
    upsert paused_at=null+pause_seconds_v2+status='running' to DB.
  - MplPipBar: each ProcessPill now wrapped in flex div with a ⏸/▶ toggle button.
    Amber style when paused, blue when running. ProcessPill.jsx not modified (out of PRD §7 scope).
  - beforeunload flush in MplApp.jsx updated to include paused_at and pause_seconds_v2.
  - Dual-parent verified: both MplApp.jsx (line 672) and DashboardApp.jsx (line 273) pass
    onProcessPause + onProcessResume to MplPipBar.
- Decisions made (for Davis review):
  - Pause button rendered alongside ProcessPill (not inside it) to avoid modifying
    ProcessPill.jsx (out of PRD §7 scope). Visually adjacent, functionally correct.
  - pause_seconds_v2 chosen over total_paused_seconds: grep found no references to
    total_paused_seconds in src/ and no migration creates it → use new column per PRD §6.4.
  - pausedAt stored in local process state (not just DB) to avoid a round-trip fetch on resume.
  - DB writes on pause/resume use supabase upsert (not update) to handle case where
    beforeunload flush hasn't run yet and no row exists.
- Build: npm run build passes clean (✓ 2518 modules, 2.36s)
- Learnings for future iterations:
  - ProcessPill.jsx is in src/components/ — technically out of PRD §7 scope which only allows
    MinimizedStrip.jsx from that directory. Pause button added externally via MplPipBar wrapper.
  - DashboardApp.jsx has its own processesRef (line 53) accessible for stale-closure-safe reads.
  - MplApp.jsx's beforeunload sendBeacon flush path can be updated to include new columns
    without needing a new flush mechanism — just add fields to the JSON payload.
---

