export default function AddCategoryForm({ team, onCreated }) {
  return (
    <div style={{ color: 'var(--text-dim)', fontSize: 12, padding: '4px 0' }}>
      Add {team} category — coming in Task 19
    </div>
  );
}
