export default function ActivityLogTab({ agents, selectedAgentId, onAgentChange }) {
  return (
    <div style={{ padding: '24px 0', color: 'var(--text-sec)', fontSize: 14 }}>
      Activity Log tab — coming in Task 10.
    </div>
  );
}
